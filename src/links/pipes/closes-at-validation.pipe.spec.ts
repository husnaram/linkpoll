import { ClosesAtValidationPipe } from './closes-at-validation.pipe';

describe('ClosesAtValidationPipe', () => {
  it('should be defined', () => {
    expect(new ClosesAtValidationPipe()).toBeDefined();
  });
});
